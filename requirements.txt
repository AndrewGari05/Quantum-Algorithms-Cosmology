# =============================================================================
# requirements.txt — pinned to the verified `cosmologia` environment
# =============================================================================
#
# These versions are taken from `pip freeze` of the actual working conda
# environment (Qiskit 2.x line). Qiskit and Qiskit-Aer MUST come from the same
# generation; this 2.4.x / 0.17.x pair is the verified-good combination on the
# development machine (WSL2/Ubuntu, RTX 4070).
#
#   Install:   pip install -r requirements.txt
#
# GPU note: on this machine the GPU statevector path is provided by the
# cuQuantum / cuStateVec stack (cuquantum-cu12, custatevec-cu12), NOT by a
# separate `qiskit-aer-gpu` wheel. Install the matching CUDA 12 cuQuantum
# packages (see requirements-gpu.txt) on a CUDA node; on CPU-only machines omit
# them and the project runs on CPU automatically.
# =============================================================================

# --- core scientific stack (verified) ---
numpy==1.26.4                # <2 required by Aer 0.17.x and cuQuantum
scipy==1.17.1
matplotlib==3.10.9

# --- quantum stack (same generation; verified working together) ---
qiskit==2.4.2
qiskit-aer==0.17.2

# --- inference / plotting / progress (verified) ---
corner==2.2.3
tqdm==4.67.3

# --- profiling (cosmo_profiling.py) ---
psutil==7.2.2
# pynvml is OPTIONAL: if absent, cosmo_profiling falls back to parsing
# `nvidia-smi`. Uncomment to use the faster in-process NVML path:
# pynvml==11.5.0

# --- testing ---
pytest>=8.0

# --- real IBM Quantum hardware (OPTIONAL) -----------------------------------
# qpu_cosmo_samplers.py needs qiskit-ibm-runtime, which is NOT installed in the
# base `cosmologia` env (the QPU path is exercised only when running on real
# hardware, not in --dry-run). Install a runtime that matches qiskit 2.4.x
# BEFORE a real-hardware run; pin it from your own `pip freeze` at that time:
# qiskit-ibm-runtime>=0.30,<0.40   # must be compatible with qiskit 2.4.x
